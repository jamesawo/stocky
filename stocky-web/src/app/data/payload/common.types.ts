import {NzButtonSize} from 'ng-zorro-antd/button';

export type CommonInputProps = {
    size?: NzButtonSize,
    title?: string,
    icon?: string,
    showTable?: boolean,
    showForm?: boolean,
}

export type CommonAddProps = {
    showForm?: boolean,
    showTable?: boolean
}
